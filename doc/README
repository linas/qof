 
                    QOF Documentation 
                    -----------------

The QOF documentation is embedded in the source code (mostly
in the header files) using 'doxygen' markup.

The documentation is built directly from the source code
when building from CVS source.

The HTML docuemtnation is placed in the 'html' subdirectory.
